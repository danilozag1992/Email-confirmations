<div class="edit-form">
<% if current_user.try(:admin?) %>
  <%= form_with(model: @post, local: true) do |f| %>
    <div class="form-group">
      <%= f.text_field :name, placeholder: "Name of project", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.text_area :body, placeholder: "Add description", class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.file_field :image, accept: "image/jpeg, image/gif,image/png", class:"form-control-file" %>
    </div>
    <%= f.submit "edit" %>
  <% end %>
<% end %>
</div>